-- Highlight other occurrences of the word under cursor.
-- NOTE: This is now handled by LSP via `vim.lsp.buf.document_highlight` in lsp.lua
return {
  --[[ "RRethy/vim-illuminate",
  event = "BufReadPost", ]]
}
